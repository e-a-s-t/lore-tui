[package]
name = "lore-core"
version = "0.1.0"
edition = "2021"
description = "Shared Lore repository logic"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2"
walkdir = "2"
